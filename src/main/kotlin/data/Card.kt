package data

//using a data class, you can define a class with one line of code! And you get build in methods like equals and toString for "free"

data class Card (val rank: String, val suite: Char) { }